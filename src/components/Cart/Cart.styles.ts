import styled from "styled-components";

export const Wrapper = styled.aside`
  width: 50rem;
  padding: 5rem 3rem;

  > p {
    padding: 2rem 0;
  }

  > ul {
    padding: 2rem 0 0;
    list-style: none;
  }

  > h3 {
    text-align: right;
    padding: 2rem 0;
  }
`;
